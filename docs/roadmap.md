# aido-templates — Roadmap

<!-- roadmap-meta
updated: 2026-08-01
-->

**Format reference:** this file is parsed by the aido app — the exact phase-heading shape, task-table columns, statuses, and section lifecycles are defined in [docs/process/roadmap.md](process/roadmap.md). Read it before editing this file by hand; the aido `/project/:name/roadmap` page follows it for you.

## Quick Updates
| Task | Area | Size | Status | Description |
|------|------|------|--------|-------------|

## Bugs
| Task | Area | Size | Status | Description |
|------|------|------|--------|-------------|

## Phase 99: Continuous Improvements — COMPLETE

### Features
| Task | Area | Size | Status | Description | Done |
|------|------|------|--------|-------------|------|
| Lean managed blocks + docs/process rollout | Templates | L | done | CLAUDE.md managed content cut from ~182 to ≤55 body lines per project: per-turn invariants + trigger-shaped pointers inline; procedures moved to new `process-*` whole-doc managed sections (`docs/process/{doc-sync,roadmap,bugs,help-sync}.md`). Generated-inventory + routing-table standards added to the doc-sync process doc. Rolled out and verified across q1dms, aido, mara, elmed, and this repo. | 2026-06-10 |
| Claude 5 template slimming | Templates | M | done | Cut 4.x-era anti-laziness/verification prodding that backfires on Claude 5 models (per Anthropic migration guidance): conventions v42 (scope bullet now guards both under- and over-delivery, harness-duplicate bullet deleted, done+report bullets merged, deferred-work sweep removed, typos), testing v17 (absolutism softened, aligned with process-bugs escape), conventions-stack-node v2 (ecosystem defaults cut, 8→4 bullets), worker-system (war story cut, red-check scoped to bug fixes/load-bearing, sections compressed, new 3-line finish-your-turns guard against mid-task stopping), teamlead-core (red-demonstration dropped, marker/divergence/roadmap-hygiene sections compressed), residuals-review skill (13-row excuse table + duplicate termination section → single 4-line termination contract). Net −82 lines (160 deleted, 78 added — additions are the compressed rewrites plus the 3-line guard). | 2026-07-25 |
| Claude 5 residuals + cross-project CLAUDE.md sweep | Templates | S | done | Post-slimming residuals per the Opus 5 / Fable 5 migration guidance: testing v18 (subagent-gate bullet made literal, "is mandatory" dropped), conventions v43 (harness-duplicate "re-read before editing" clause cut); stale `~/Apps/aido/templates` editing-flow path in this repo's CLAUDE.md corrected. v43/v18 hand-propagated byte-identical to aido, mara, q1dms, q1erp (verified, zero drift). Same sweep delivered unmanaged-section cleanups outside this repo: q1dms (bug post-mortem lore trimmed to rules), hn (Komodo secret externalized to `~/.config/komodo/claude.env`, resolved-incident stories relocated to learnings), cliplus (init-era doc compressed + conventions block adopted), aido-ops (hand-forked conventions replaced with managed conventions + conventions-stack blocks). | 2026-07-26 |
| Managed-block harmonization pass | Templates | S | done | Opus 5 re-read of the in-use blocks: testing v20 ("real surface" pinned to the tier that fits the change per `docs/tests.md`, explicitly not necessarily E2E — the prior wording pulled agents toward E2E on trivial changes, contradicting the tests.md tier table), conventions v45 (the `'Checkpoint' = commit and continue` rule now states that it overrides the harness default to commit only when asked). Then conventions v46 (worktree rule now states it is a standing owner instruction that holds even when a harness/launcher configured the session to work in place — a background job with `bgIsolation: none` otherwise reads "work in place" and lands dev work on the default branch; "main" generalized to "the repo's default branch"; already-isolated agents explicitly stay put) and active-work v6 (markdown bug fix: no blank line before the closing italic meant it rendered inside the "Never relegate" bullet; plus verbosity trims, −6%. Deeper compression was drafted and rejected — it saved only 14% while cutting two clauses that carry unique meaning). | 2026-07-26 |
| Post-superpowers gap fill | Templates | S | done | Superpowers plugin evaluated against the Claude 5 runcards and retired (autonomy conflicts, unrequested commits, SDD token overhead, 4.x-era pressure prompting); the three gaps it left filled: new `debugging` skill (root-cause-first, one hypothesis at a time, boundary instrumentation, three-failed-fixes escalation), testing v19 (a new test must be seen to fail — write-first or revert-check), conventions v44 (verify review feedback against the codebase before implementing). Skill installed to `~/.claude/skills/debugging/`; v19/v44 to propagate to consuming projects via drift. | 2026-07-26 |
| Fleet audit: template fixes + project cleanup | Templates | M | done | Three-agent audit of all templates + seven project folders (engagement prompts vs live aido code findings recorded but deliberately untouched — recently reworked). Template fixes: conventions v52 (restored scope-persistence + checkpoint contracts) then v53 (CLAUDE.md/managed-block edit guard), tests v10 (placeholder table moved out of the managed body into the scaffold's project-owned tail; cross-repo governance section dropped), process-roadmap v5 (malformed heading), active-work v8 + program-prep skill (resume-prompt rename). Project cleanups committed in aido (dead help/seeding blocks + orphan process doc + RECOVERY.md removed, ship-rule dupe dropped), q1dms (footnotes-merge cursor repair incl. duplicated block marker, roadmap-meta 7.5 KB→2.4 KB with narratives archived, AGENTS.md alias), q1erp (tests block filled with real runners section, resume-prompt rename), aido-ops (managed active-work block added, knowbs/aidev-era roadmap head replaced with process-roadmap v5 pointer, dead pending-asks loop closed, runtime-state gitignore gap fixed). All four also synced to conventions v53 + tests v10. Follow-up autonomous pass (2026-07-30): mara catch-up (v53 + tests v10 + H1/intro, active-work cursor created, AGENTS.md alias, bug-reports merged into docs/bug-hunts/), cliplus v53 sync, and a 12-agent roadmap diet — 359 done-row postmortem cells (aido 68, q1dms 228, q1erp 63) trimmed to one-line summaries with originals archived verbatim in each project's roadmap-completed.md (aido 371→257 KB, q1dms 944→403 KB, q1erp 415→307 KB); phase-archival decisions + q1erp generated-inventory adoption filed as rows in their own roadmaps. | 2026-07-29 |
| Phase archival rule + fleet line-draw | Templates | S | done | process-roadmap v6: new § Phase archival — a phase reaching COMPLETE moves wholesale to roadmap-completed.md in the same commit (registry row stays in ## Completed Work; Phase 99 exempt; numbers never reused), plus a self-healing sweep clause for phases flipped COMPLETE from the aido UI, so no periodic job is needed. v6 hand-synced to aido, mara, q1dms, q1erp, aido-ops and this repo's own copy (which was still v4). Line drawn the same day: 77 COMPLETE phases archived fleet-wide (aido 20, q1dms 30, q1erp 14, mara 13), duplicate registry rows deduped against pre-existing Completed Work entries; active roadmaps now 182/276/233/62 KB. | 2026-07-30 |
| Slice contract: sizing + one-assignment execution | Templates | S | done | Fix for agents stopping mid-slice during active-work execution, done by defining the noun once: active-work v9 (Sequence items defined as session-sized slices grouped by shared context; new "a slice is one assignment" bullet — run end-to-end, merge, refresh the cursor, report once with merge state + whether a next slice remains), conventions v55 (generic granularity bullet: the assignment is the whole handed batch, not one step of it), program-prep skill (explicit one-session sizing criterion for slices). Engagement workers already had this contract via worker-system's finish-your-turns; this extends it to solo sessions through the guidance block both paths treat as the rulebook. | 2026-07-30 |
| Claude 5 word-level compaction pass | Templates | S | done | Same rules, fewer tokens across 8 sections: testing v21 (done-definition bullets merged, agent's-job line left to conventions, rationale + procedure-preview cuts), help v5 (regression rule stated once instead of three times across block + process doc), roadmap v17 + documentation-sync v11 (pointed-doc previews and motivation sentence cut), tests v11 (CLAUDE.md-duplicate skill pointer dropped, isolation lead de-prodded, inventory paragraph tightened), process-bugs v2 (assertion-shape bullet deduped vs tests.md and its own step 2), process-doc-sync v2 + process-help-sync v2 (light trims; form-fields/semantics bullets merged). Owner-approved follow-up: conventions v56 (question-answering clarity sentences merged; simplicity bullets merged with polarity fix — "prefer bug-proneness" → robustness), process-roadmap v7 ("is now derived" artifact dropped; Potential Improvements description requirement compressed), this repo's own process-roadmap copy synced to v7. Deliberately untouched: active-work (density evaluated at v6), seeding, conventions-stack, scaffolds. | 2026-07-30 |
| Engagement prompt corrections (unfrozen) | Prompts | S | done | Owner unfroze; every claim re-verified against aido source before editing (10 confirmed, 1 refuted, 1 partial). Applied: teamlead-deny-list → `aido.proposeApproval`; teamlead-program `spawnWorker` examples carry the required `projectName`; worker-system strike rule scoped program-vs-loop (the file is appended to every worker spawn with no mode branch; loop workers own the cursor), subagent tiering made model-neutral (tiers are operator-configured incl. fable), "one task" → "one assignment"; approval-rules names the engagement teamlead as approval authority (`isApprovalAuthorityIn`); decision-rules-worker drops the unenforceable "mention only @teamlead" (`handle-agent-reply` auto-implies @user on DECISION blocks); participant-joined's escalation line made role-neutral; protocol-rules permits DECISION/PROPOSAL alongside REPLY (parser collects all blocks per buffer); bug-hunt team create/shutdown replaced with plain parallel subagents (createTeam/shutdownTeam never existed — the session is a bare PTY with no aido.* surface); standing-preferences body emptied so state-preamble's length guard skips injection; residuals-review skill defaults to single cycle, loops only on request (reviewer-runner invokes it on every spawn). Refuted, dropped: teamlead-planner dual protocols are code-required (tasks mode cannot passComplete; a structural test pins both sections). aido-repo residue filed in aido's Quick Updates (aido `6adb1e8c`): proposeEnd lacks a mode gate; `aido.runResidualsReview` documented nowhere; teamlead-prompts.test.ts pins the old standing-preferences shape (will fail until updated); state-preamble's empty-case test now covers a reachable state. This repo's CLAUDE.md editing flow corrected: no `templates:sync` — dev resolves the templates root live. | 2026-07-30 |
| Capability-management source-of-truth contract | Templates | S | done | Aligned the Skills authoring guide and agent-governance policy with the consolidated aido surface: `/skills` authors sources and observes physical state read-only; `/skills/governance` alone manages supported host capabilities from native Claude/Codex and selected-project sources, with reviewed actions and fresh semantic re-observation; cache registries, a second config writer, and SQLite capability state are explicitly excluded. | 2026-08-01 |

## Distant Roadmap
| Task | Area | Size | Status | Description |
|------|------|------|--------|-------------|

## Potential Improvements
| Task | Area | Size | Status | Description |
|------|------|------|--------|-------------|
| syncAll section gating per project | Templates | M | postponed | May improve: `syncAll` currently offers every section to every project (drift shows "missing" for blocks a project deliberately lacks, e.g. aido-templates without `testing`); a per-project section allow-list would make "missing" meaningful and prevent an accidental full-sync from injecting `help`/`seeding` into docs-only repos. Cost: M (schema or per-project config + UI). Not done now: out of scope for the 2026-06-10 lean-blocks rollout, which only touched content, not sync machinery. |

## Completed Work
| Phase / Feature | Summary |
|----------------|---------|
| Lean managed blocks + docs/process rollout (2026-06-10) | Managed CLAUDE.md diet across all aido-managed projects; process-* section family introduced. |
