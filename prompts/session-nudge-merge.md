---
category: sessions
description: Sent when the user clicks "Ask the agent to continue" on a stalled merging session.
variables: [base]
---

The merge into "{{base}}" is not finished yet. Please continue: commit any remaining work, resolve any conflicts, run the tests, then merge this branch into "{{base}}" so the work lands.
